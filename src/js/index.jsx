import React from 'react';
import ReactDOM from 'react-dom';
import Application from 'js/Application';

const rootDiv = document.querySelector('body > div');
ReactDOM.render(<Application />, rootDiv);
